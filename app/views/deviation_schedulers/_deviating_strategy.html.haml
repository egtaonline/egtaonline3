%tr
  %td= deviating_strategy
  %td= link_to 'Remove Deviating Strategy', "/schedulers/#{scheduler.id}/roles/#{role}/remove_deviating_strategy?strategy=#{deviating_strategy}", method: :post, class: 'btn btn-mini btn-warning'