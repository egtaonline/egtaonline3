%tr
  %td= strategy
  %td#add_or_remove_strategy_link= link_to 'Remove Strategy', "/simulators/#{simulator.id}/roles/#{role_name}/remove_strategy?strategy=#{strategy}", method: :post, class: 'btn btn-mini btn-warning'
