= simple_form_for scheduler, html: {class: 'form-inline' } do |f|
  = f.input :name
  = f.input :size, label: "Game size"
  = f.input :default_observation_requirement
  = f.input :observations_per_simulation
  = f.input :process_memory, label: "Process memory (in MB)"
  = f.input :time_per_observation, label: "Time per observation (in seconds)"
  = f.input :nodes, label: "Nodes required"
  = f.input :active, as: :boolean
  = render partial: "configuration/configuration", object: scheduler.simulator_instance.configuration
  = f.button :submit, 'Update Scheduler'