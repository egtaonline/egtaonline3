%table.table.table-striped
  %thead
    %tr
      %th= sortable "state"
      %th= sortable "profiles.assignment", "Profile"
      %th= sortable "simulator_fullname", "Simulator"
      %th= sortable "id", "Folder Number"
      %th= sortable "job_id"
  %tbody
    =render simulations
=paginate simulations, remote: true
