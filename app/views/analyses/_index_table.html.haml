%table.table.table-striped
  %thead
    %tr
      %th= sortable "status"
      %th= sortable "game_id", "Game"
      %th= sortable "id", "Folder Number"
      %th= sortable "job_id"
      %th= sortable "created_at", "Time Created"

  %tbody
    = render analyses
= paginate analyses, remote: true
